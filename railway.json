{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python app/migrate.py && streamlit run app/app.py --server.port $PORT --server.address 0.0.0.0 --server.fileWatcherType none --browser.gatherUsageStats false"
  }
}
